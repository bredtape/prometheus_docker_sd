# my global config
global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
# alerting:
#   alertmanagers:
#   - scheme: http
#     static_configs:
#     - targets:
#       - "alertmanager:9093"

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  - job_name: "file_sd"
    file_sd_configs:
      - files:
          - ./file_sd.json
